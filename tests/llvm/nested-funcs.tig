let
  function add(a : int, b : int) : int = a + b
  function mul(a : int, b : int) : int = a * b
  function square(n : int) : int = mul(n, n)
in
  print_int(add(3, 4)); print("\n");
  print_int(mul(3, 4)); print("\n");
  print_int(square(5)); print("\n")
end
